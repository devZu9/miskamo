# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
.eggs/

# Environments
.env
.venv
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Project - large binary files
libs/FluidR3_GM.sf2
libs/*.dll

# Windows native libs (pyfluidsynth deps) — install via install_windows.bat or Homebrew on Mac
*.dll

# Project - generated directories
_tmp/
_output/
_shared/_dataset/
_shared/_train_output/
_shared/_output/
_shared/_tmp/
_shared/_midi_banks/
_shared/_corrupt_presets/
_shared/_midi_gen_presets/

# Project - submodules
DDSP-Timbre-Transfer/

# Project - settings (user-specific)
settings.json
ratings.csv

# MIDI banks — keep only empty generated/ on GitHub
_midi_banks/*
!_midi_banks/generated/
_midi_banks/generated/*.mid

# Corrupt presets — keep only defaults on GitHub
_corrupt_presets/*
!_corrupt_presets/heavy.json
!_corrupt_presets/light.json
!_corrupt_presets/medium.json

# MIDI gen presets — keep directory on GitHub but presets local only
_midi_gen_presets/*
!_midi_gen_presets/.gitkeep
